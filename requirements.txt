# Sürümler SABİT: 2026-07-24'te 12/12 testin geçtiği ortam bu.
# Neden sabit? transformers 4.x→5.x geçişi API değiştirdi ve kodu kırdı (bkz. embedder.py
# pooler_output notu). Gevşek aralık ("transformers>=4.35") gelecekte aynı sınıf sorunu üretir.
# Sürüm yükseltmek isteyen: sürümü değiştir → pytest tests/ → yeşilse commit'le.
# torch/torchvision CPU indeksinden kurulur: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
torchvision==0.28.0
transformers==5.14.1
protobuf==5.29.3
Pillow==12.2.0
numpy==2.4.4
fastapi==0.139.2
uvicorn==0.51.0
python-multipart==0.0.32
google-cloud-vision==3.15.0
python-dotenv==1.2.2
httpx==0.28.1
pydantic==2.13.4
pytest==9.1.1
# RabbitMQ istemcisi. Senkron (BlockingConnection) olan bilinçli seçim:
# model çıkarımı CPU'ya bağlı senkron bir iş, asenkron istemci kazandırmaz.
pika==1.4.2
